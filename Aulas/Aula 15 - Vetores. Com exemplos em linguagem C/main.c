#include <stdio.h>

int main() {
    int vet[10];
    unsigned int cont;

    for(cont=0; cont<10; cont++) {
        vet[cont] = 0;
    }

    //Ao final do loop, cada uma das 10 posições terá o valor 0 (zero).

    return 0;
}