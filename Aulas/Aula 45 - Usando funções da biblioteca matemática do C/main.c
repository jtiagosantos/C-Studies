#include <stdio.h>
#include <math.h>

int main() {
    float x = 18.5;
    float y = 3.35;

    printf("A raiz quadrad a de %.1f eh %.1f\n", x, sqrt(x));
    printf("O valor eh: %.1f\n", pow(2.0, y));

    return 0;
}